# Frontend build-time config. Add this variable to the frontend service in
# Render before building. Override it with localhost for local development.
VITE_API_URL=https://quizportal-c2dz.onrender.com/api
